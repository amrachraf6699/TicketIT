models:
  User:
    properties:
      id:
        type: integer
        format: int64
      name:
        type: string
      email:
        type: string
        format: email
      password:
        type: string
        format: password
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
